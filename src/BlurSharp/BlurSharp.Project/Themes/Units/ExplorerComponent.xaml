<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:james="https://jamesnet.dev/xaml/presentation"
    xmlns:units="clr-namespace:BlurSharp.Project.UI.Units">
    <Style TargetType="{x:Type units:ExplorerComponent}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type units:ExplorerComponent}">
                    <james:JamesGrid Columns="*" Rows="auto, 1,*">
                        <units:IconLable
                            Command="{Binding OpenDialogCommand}"
                            Content="폴더열기"
                            IconType="FolderOpenOutline" />
                        <Border HorizontalAlignment="Stretch" Background="White" />
                        <units:FolderTreeView ItemsSource="{Binding Files}" />
                    </james:JamesGrid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>